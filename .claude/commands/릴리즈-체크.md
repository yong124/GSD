# 릴리즈 체크

이 명령은 아래 순서로 움직인다.

1. `git status --short`
2. 변경 축에 맞는 검증 실행
3. `?v=` 누락 여부 확인
4. generated / handoff 갱신 필요 여부 판단
5. temp 폴더 정리
6. 커밋 범위 요약

우선 사용할 스킬:

- `gsd-release-checks`

필요 시 같이 사용할 스킬:

- `gsd-data-polish`
- `gsd-runtime-ui`
- `gsd-editornode-pipeline`
